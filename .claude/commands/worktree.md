---
description: Creates a worktree from an existing branch or creates a new one.
allowed-tools: Bash, Edit
argument-hint: [existing branch ID]
---

# Command: /worktree

Create a new worktree in the project folder (not the main branch's folder) using the branch ID provided. If no branch ID is provided, create a new worktree based on the new feature described. If no new feature or ID is provided, STOP and ask me what I want to do.
